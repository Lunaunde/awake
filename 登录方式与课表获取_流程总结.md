# 华南理工教务系统：两种登录方式 → 获取课表 全流程总结

- 版本：v1.0 ｜ 日期：2026-08-28 ｜ 目标系统：`xsjw2018.jw.scut.edu.cn`（正方教务）＋ `sso.scut.edu.cn`（CAS 统一认证）
- 适用账号：学生本人账号（示例学号已脱敏）

---

## 0. 总览

```
┌──────────────────┐    ┌─────────────────────┐
│ ① 微信扫码登录     │    │ ② 账号密码登录        │
│ (OAuth + 轮询)    │    │ (3DES 表单 + 可选2FA) │
└────────┬─────────┘    └──────────┬──────────┘
         └──────────┬──────────────┘
                    ▼
        CAS 票据链（两种方式汇合，完全相同）
        qRCode/ticket → driotlogin → ticketlogin
                    ▼
        教务会话 Cookie（JSESSIONID /jwglxt）
                    ▼
        课表数据接口（kbcx/xskbcx_cxXsKb.html）→ JSON
```

---

## 1. 登录方式 ①：微信扫码登录

| 步骤 | 操作 | 关键参数/说明 |
|---|---|---|
| 1 | `GET https://sso.scut.edu.cn/cas/login?service=<教务入口>` | 加载扫码 JS（login-qrcode.js） |
| 2 | 前端生成 UUID（v4 格式），拼微信 OAuth 二维码 | `appid=wx39f121ed798af736`、`redirect_uri=https://sso.scut.edu.cn/cas/scutwxsso`、`scope=snsapi_base`、`state=<UUID>` |
| 3 | 用户用微信扫码（**需先绑定华工账号**） | 微信 OAuth 回调带 `state=<UUID>` 标记服务端 |
| 4 | 轮询 `GET /cas/scutqqcheck?uuid=<UUID>&jsonpcallback=cb` | 每 2s 一次；`cb(-1)`=未扫；返回 `cb("<token>")`=成功 |
| 5 | `GET /cas/qRCode?token=<token>&service=<编码后的教务入口>` | **302 + `Set-Cookie: CASTGC`** + `Location` 携带 `ticket=ST-xxx` |

> 要点：UUID 由客户端生成（服务端无预注册接口）；二维码轮询窗口约 120s（60×2s），但扫码成功后服务端会保留 token，稍后轮询仍可取；`qRCode` 是两种方式共用的"换会话"入口。

---

## 2. 登录方式 ②：账号密码登录（含二次认证分支）

| 步骤 | 操作 | 关键参数/说明 |
|---|---|---|
| 1 | `GET /cas/login?service=<编码后的 service>` | 解析隐藏字段 `lt`（登录票据）、`execution`（初始为 `e1s1`） |
| 2 | 3DES 加密（服务器原版 des.js）：`rsa = strEnc(用户名 + 密码 + lt, '1','2','3')` | `ul`=用户名长度、`pl`=密码长度，供服务端解密后拆分 |
| 3 | `POST /cas/login?service=...` 表单：`rsa, ul, pl, lt, execution, _eventId=submit` | 图形验证码/滑块当前关闭（`isCode='0'`、`is_open_captcha='0'`） |
| 4 | **分支 A**（教务等旧服务）：→ **302** + CASTGC + ticket | 直接进入第 5 节票据链 |
| 4' | **分支 B**（个人门户等服务，有二次认证）：→ **200 二次认证页** | 见下方「二次认证」 |
| 5 | 进入第 3 节票据链 | — |

### 二次认证（企业微信/短信验证码）

服务端要求时返回二次认证页（`execution` 变为 **`e1s2`**，LT 复用）：

- 验证码：经**微信企业号"华南理工大学"消息通知或手机短信**下发，**90 秒**有效，字段 `PM1`
- 提交：再次 `POST /cas/login?service=...`，携带 `PM1=<验证码>`、`lt`、`execution=e1s2`、`_eventId=submit` → **302 + CASTGC**
- "重新获取验证码"= 90s 倒计时后刷新页面（无独立接口，`toRecheckLogin.js`）；有每日获取次数上限
- **信任机制**：同一客户端完成一次验证后，一段时间内再登录免二次认证（实测：验证后直接 302 通过）
- 触发范围实测：教务 `xsjw2018` service **不触发**；个人门户 `my.scut.edu.cn/up/` **触发**（可理解为按 service + 客户端信任状态动态判定）

---

## 3. 汇合点：CAS 票据链与最终 Cookie

两种登录方式均以"302 + `ticket=ST-xxx`"进入同一跳转链：

| # | 请求 | 响应 | 下发 |
|---|---|---|---|
| 1 | `GET /sso/driotlogin?ticket=ST-xxx` | 302 | `JSESSIONID`（Path=/sso） |
| 2 | `GET /sso/driotlogin` | 302 | → `/jwglxt/ticketlogin?uid=<学号>&timestamp=<秒>&verify=<32位hex>`（verify 由服务端生成，客户端原样携带） |
| 3 | `GET /jwglxt/ticketlogin?uid=...&timestamp=...&verify=...` | 302 | **`JSESSIONID`（Path=/jwglxt，教务主会话）** |
| 4 | `GET /jwglxt/xtgl/login_slogin.html` | 302 | → `/jwglxt/xtgl/index_initMenu.html?jsdm=xs&_t=...` |
| 5 | `GET /jwglxt/xtgl/index_initMenu.html?jsdm=xs` | **200** | 登录成功 |

**最终 Cookie 清单**（域/Path 决定作用域）：

| Cookie | 域 | Path | 作用 |
|---|---|---|---|
| `CASTGC` | sso.scut.edu.cn | `/cas` | CAS 全局会话 |
| `JSESSIONID` | xsjw2018.jw.scut.edu.cn | `/sso` | 票据验证会话 |
| `JSESSIONID` | xsjw2018.jw.scut.edu.cn | `/jwglxt` | **教务主会话（抓课表必须）** |
| `clwz_blc_pst_*` | 各域 | `/` | 风控标记（非必需） |

---

## 4. 获取课表

前置：持有 `/jwglxt` 的 `JSESSIONID`（两种登录方式最终都一样）。

| 项 | 内容 |
|---|---|
| 数据接口 | `GET http://xsjw2018.jw.scut.edu.cn/jwglxt/kbcx/xskbcx_cxXsKb.html?xnm=<学年>&xqm=<学期>&xszd=&kblx=1` |
| 请求头 | `Cookie: JSESSIONID=...`；建议 `Referer: /jwglxt/kbcx/xskbcx_cxXskbcxIndex.html` + 浏览器 UA |
| `xnm` | 学年起始年：`2025` = 2025-2026 学年 |
| `xqm` | 学期码：`3`=第1学期，`12`=第2学期，`16`=小学期 |
| `kblx` | `1`=学生课表 |
| 返回 | JSON：`xsxx`（学生信息）、`kbList`（理论课）、`sjkList`（实践课） |

**kbList 单条常用字段**：`kcmc`(课程)、`xm`(教师)、`cdmc`(教室)、`xqj/xqjmc`(星期)、`jcs`(节次)、`zcd`(周次)、`xf`(学分)、`zxs`(总学时)、`kclbmc`(课程性质)、`khfsmc`(考核方式)、`jxbmc`(教学班号)。

**实测样例**（2026-08-28）：

| 学年学期 | xnm/xqm | 理论课 | 实践课 |
|---|---|---|---|
| 2025-2026 第2学期 | 2025/12 | 16 条 | 7 条 |
| 2026-2027 第1学期 | 2026/3 | 13 条 | 4 条 |
| 2025-2026 小学期 | 2025/16 | 0 | 0 |

> 备注：课表查询**首页** `/kbcx/xskbcx_cxXskbcxIndex.html` 当日返回 500（系统升级维护页），但**数据接口不受影响**。

---

## 5. 最小复现（curl 关键命令）

```bash
# —— 扫码登录 ——
# 二维码内容 = 微信 OAuth 链接（state=<UUID>），用户扫码后：
curl -sS "https://sso.scut.edu.cn/cas/scutqqcheck?uuid=<UUID>&jsonpcallback=cb"   # → cb(-1) 或 cb("token")
curl -sS -L -c cookies.txt "https://sso.scut.edu.cn/cas/qRCode?token=<token>&service=http%3A%2F%2Fxsjw2018.jw.scut.edu.cn%2Fsso%2Fdriotlogin"

# —— 密码登录（rsa 需 3DES，参考 scut_pwd_login.mjs；此处为提交后的结果） ——
# POST /cas/login?service=...  表单: rsa/ul/pl/lt/execution(e1s1)/_eventId=submit
#（若二次认证: 再 POST 一次, 追加 PM1=验证码, execution=e1s2）

# —— 两种方式汇合后 ——
curl -sS -L -c cookies.txt "https://sso.scut.edu.cn/cas/qRCode?token=<token>&service=..."   # 扫码路径
# 或密码路径直接跟随 POST 返回的 302 → driotlogin?ticket=ST-xxx → ticketlogin（curl -L 自动跟随）

# —— 抓课表 ——
curl -sS -b cookies.txt -A "Mozilla/5.0" \
  -e "http://xsjw2018.jw.scut.edu.cn/jwglxt/kbcx/xskbcx_cxXskbcxIndex.html" \
  "http://xsjw2018.jw.scut.edu.cn/jwglxt/kbcx/xskbcx_cxXsKb.html?xnm=2025&xqm=12&xszd=&kblx=1" -o kbcx.json
```

---

## 6. 脚本与文件清单（scut_qr 目录）

| 文件 | 说明 |
|---|---|
| `gen_qr.py` | 生成登录二维码 PNG（本地 qrcode 库） |
| `poll_and_login.py` | 扫码轮询脚本（Python；注意该 CAS 与 Python 默认 TLS 握手失败，见下） |
| `scut_pwd_login.mjs` | 密码登录（无 2FA 服务）：完整票据链 + 存 Cookie + 可选抓课表 |
| `scut_pwd_mfa_login.mjs` | 密码 + 二次认证（两步：`--step1` 触发验证码推送 → `--step2 <码>` 完成） |
| `cookies.txt` / `cookies_pwd.txt` | 各次登录的 Netscape 格式 Cookie |
| `kb_xnm=2025_xqm=12.json` 等 | 各学期课表原始 JSON |

---

## 7. 注意事项

1. **TLS**：该 CAS 与 Python 3.13 默认 SSL 握手失败（`SSLV3_ALERT_HANDSHAKE_FAILURE`）；PowerShell / curl / Node 正常。
2. **验证码状态**：当前图形/滑块验证码关闭；若风控触发（异地、密码多次错误等）动态开启，脚本会明确报错。
3. **二次认证**：按 service + 客户端信任动态触发；验证码 90s 有效、有每日上限；频繁触发可能限流。
4. **Cookie 即凭证**：勿外传；避免对教务接口高频并发（`clwz_blc_pst_*` 风控）。
5. **合规**：仅限本人账号自动化与学习研究，遵守学校信息化管理规定。